<%= @broadcast.text %>

---

You receive this email, because you are subscribed to broadcast messages by the TinyMon admin at http://<%= TinyMon::Config.host %>. To disable these email notifications in the future, log into TinyMon at http://<%= TinyMon::Config.host %><%= login_path %> and the go to http://<%= TinyMon::Config.host %><%= settings_path %>.
