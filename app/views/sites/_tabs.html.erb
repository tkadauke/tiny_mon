<%= tabs(:selected => selected) do |a| %>
  <% a.tab :information, t(".information"), account_site_path(@account, @site) %>
  <% a.tab :health_checks, t(".health_checks"), account_site_health_checks_path(@account, @site) %>
  <% a.tab :deployments, t(".deployments"), account_site_deployments_path(@account, @site) %>
  <% a.tab :edit, t(".edit"), edit_account_site_path(@account, @site) %>
<% end %>
