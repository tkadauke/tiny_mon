<p class="form_item">
  <span class="label"><%= f.label :name, t('.status'), :class => 'required' %></span>
  <%= f.text_field :status %>
</p>

<p><%= t('steps.page_must_be_loaded_before_this_step') %></p>
