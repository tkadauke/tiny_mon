<% title t('.settings') %>

<div class="box">
  <div class="box-body">
    <%= form_for :configuration, :url => settings_path, :html => { :class => 'form-horizontal' } do |f| %>
      <%= render :partial => "/settings/form", :locals => { :config => @configuration, :f => f } %>

      <p>
        <%= f.submit t('.update'), :class => 'btn btn-primary btn-small btn-flat' %>
      </p>
    <% end %>
  </div>
</div>
