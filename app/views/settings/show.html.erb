<h2><%= t('.settings') %></h2>

<%= form_for :configuration, :url => settings_path, :html => { :class => 'form' } do |f| %>
  <%= render :partial => "/settings/form", :locals => { :config => @configuration, :f => f } %>
  <p><%= f.submit t('.update') %></p>
<% end %>
