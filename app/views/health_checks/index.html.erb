<h2><%= t('.health_checks_for_site', :site => @site.name) %></h2>

<p>
  <%= link_to t('health_checks.report.details'), site_health_checks_path(@site, :report => 'details') %> |
  <%= link_to t('health_checks.report.overview'), site_health_checks_path(@site, :report => 'overview') %>
</p>

<%= auto_update 'checks' %>
<div id="checks">
  <%= render :partial => "list" %>
</div>

<p><%= link_to t('.new_health_check'), new_site_health_check_path(@site) %></p>
