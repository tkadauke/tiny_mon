<h2><%= t(".mark_deploment_of_site", :site => @site.name) %></h2>

<%= form_for [@account, @site, @deployment], :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :revision, t(".revision"), :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :revision %>
      <p><small><%= t(".optional_revision_number") %></small></p>
    </div>
  </div>
  
  <div class="control-group">
    <%= f.label :schedule_checks_in, t(".schedule_checks_in"), :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :schedule_checks_in %>
      <p><small><%= t(".when_to_schedule_checks_in") %></small></p>
    </div>
  </div>
  
  <div class="control-group">
    <div class="controls">
      <%= f.submit t(".mark_deployment"), :class => 'btn' %>
    </div>
  </div>
<% end %>
