<div class="control-group">
  <%= f.label :url, t('.url'), :class => 'control-label required' %>
  <div class="controls">
    <%= f.text_field :url, :class => 'form-control' %>
    <p><small><%= t('.absolute_url') %></small></p>
  </div>
</div>

<div class="control-group">
  <%= f.label :negate, t('.negate'), :class => 'control-label required' %>
  <div class="controls">
    <%= f.check_box :negate, :class => 'form-control' %>
  </div>
</div>

<p><%= t('steps.page_must_be_loaded_before_this_step') %></p>
